var Promise = require("promise");
var mongodb = require('mongodb')
mongodb.connect("mongodb://localhost:27017/attendance");


var promise = new Promise((resolve,reject)=>{
    var a
    if (a!=null)
    {
        resolve()
    }
    else{
        
    }
})
